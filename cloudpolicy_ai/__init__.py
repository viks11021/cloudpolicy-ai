"""
cloudpolicy-ai
A Terraform GCP infrastructure compliance scanner with an optional
Vertex AI (Gemini) explanation layer.

Companion project to compliance-scanner (AWS/Anthropic). This one targets
GCP resource types and uses Vertex AI's Gemini models, with real GCP
project + Application Default Credentials auth rather than a simple API key.
"""

__version__ = "0.1.0"
